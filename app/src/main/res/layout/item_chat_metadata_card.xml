<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/metadata_root_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="10dp"
    android:layout_marginEnd="10dp"
    android:layout_marginTop="4dp"
    android:layout_marginBottom="4dp"
    android:orientation="vertical"
    android:padding="6dp"
    android:background="@drawable/bg_context_card"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/metadata_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="10sp"
            android:fontFamily="sans-serif-medium"
            tools:text="\uD83D\uDCF1 WhatsApp \u00B7 \uD83D\uDC64 Mom \u00B7 \uD83D\uDE0A Happy" />

        <ImageView
            android:id="@+id/metadata_expand_icon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:src="@drawable/down_arrow_icon"
            android:alpha="0.5"
            android:contentDescription="Expand" />
    </LinearLayout>

    <TextView
        android:id="@+id/metadata_details"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:fontFamily="monospace"
        android:textSize="10sp"
        android:visibility="gone"
        tools:text="{sender: 'Mom', message: 'Beta dinner pe aao'}"
        tools:visibility="visible" />
</LinearLayout>
