<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- This is the color of the ripple effect -->

    <!-- This is an optional mask. It defines the shape of the ripple.
         A simple rectangle is fine here. -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="8dp" /> <!-- Optional: rounded corners for the ripple shape -->
        </shape>
    </item>

    <!-- This is the normal, non-pressed background. We can leave it transparent
         or set a color. We will set the color programmatically, so transparent is best. -->
    <item android:drawable="@android:color/holo_blue_bright" />

</ripple>